# A library that provides a single function, that of the same name

def assert (condition) {
    # Assert that condition is True by
    # dividing on it (If it's False then zero devision error)
    
    for (1/condition: -1) {} # To evaluate it at runtime
}